__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store
*.log

# scraped data + caches live on orphan `data-<region>` branches, never on main
# (the pre-split `data` branch is only a fallback); locally they sit untracked
site/data/
site/region/
cache/
